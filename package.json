{
  "name": "reveal-bot-nft-quant-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/main.js",
    "test": "tsx --test test/**/*.test.ts",
    "check": "tsc -p tsconfig.json --noEmit",
    "audit:runtime": "npm audit --omit=dev"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "4.23.1",
    "typescript": "7.0.2"
  }
}
